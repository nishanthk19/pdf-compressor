{
  "name": "pdf-target-compressor",
  "version": "1.0.0",
  "description": "Compress uploaded PDFs to a target file size in MB using Ghostscript binary search",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "echo 'Build complete'"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
